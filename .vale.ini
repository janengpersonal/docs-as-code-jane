# .vale.ini — minimal, safe config

StylesPath = styles
MinAlertLevel = warning

# Map MDX files to Markdown so Vale can lint them
[formats]
mdx = md

# Only apply rules to Markdown/MDX
[*.md]
BasedOnStyles = Microsoft

[*.mdx]
BasedOnStyles = Microsoft

